services:
  mqtrader:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      TRADER_NODE_CONFIG: "/app/config/node_config.json"  # 容器内的路径
      STRATEGY_CONFIG: "/app/config/SOL-USDT-SWAP.OKX.json"  # 容器内的路径
      STRATEGY_LIB: "/app/strategies/libmq_demo_strategy"  # 容器内的路径
    volumes:
      - ./config:/app/config:ro  # 将主机的 config 目录挂载到容器的 /app/config 目录，确保容器可以读取配置文件
      - ./strategies:/app/strategies:ro  # 将主机的 strategies 目录挂载到容器的 /app/strategies 目录，确保容器可以读取动态库
      - /Volumes/wdata/mountainsea-lab/mq-strategy-L/strategy:/Volumes/wdata/mountainsea-lab/mq-strategy-L/strategy
# mqtrader
mqtrader 是一个基于 nautilus_trader 库封装的量化交易系统，旨在提供一个高效、灵活的框架来进行策略开发、实时交易、模拟交易
# 构建命令
> 构建镜像
``` shell
    docker-compose build --no-cache
```
> 后台运行
```shell
     docker-compose up -d
```
> 日志查看
```shell
     docker-compose logs -f
```
> 停止运行
```shell
     docker-compose down
```